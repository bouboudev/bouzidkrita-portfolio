---
import Layout from '../layouts/Layout.astro';
import Icon from '../components/Icon.astro';
import { SITE } from '../data/site';
---

<Layout title="Contact — Bouzid Krita" description="Contacter Bouzid Krita : email, LinkedIn, GitHub, formulaire.">
  <section class="wrap max-w-2xl py-14 md:py-20">
    <p class="eyebrow">Contact</p>
    <h1 class="title-xl mt-4">Un besoin ? Parlons-en.</h1>
    <p class="muted mt-4 leading-relaxed">Développement, support applicatif, intégration, maintenance : je réponds vite et simplement.</p>

    <nav class="social-row mt-8" aria-label="Me retrouver en ligne">
      <a class="social-btn" href={SITE.github} target="_blank" rel="noopener noreferrer" data-umami-event="github-click">
        <Icon name="github" /> GitHub
      </a>
      <a class="social-btn" href={SITE.linkedin} target="_blank" rel="noopener noreferrer" data-umami-event="linkedin-click">
        <Icon name="linkedin" /> LinkedIn
      </a>
      <a class="social-btn" href={`mailto:${SITE.email}`}>
        <Icon name="mail" /> {SITE.email}
      </a>
    </nav>

    <form name="contact" method="POST" action="/merci/" data-netlify="true" netlify-honeypot="bot-field" class="card mt-10 space-y-5">
      <input type="hidden" name="form-name" value="contact" />
      <p class="hidden" aria-hidden="true"><label>Ne pas remplir : <input name="bot-field" tabindex="-1" autocomplete="off" /></label></p>
      <div class="field">
        <label for="name">Nom</label>
        <input id="name" name="name" required autocomplete="name" minlength="2" maxlength="100" />
      </div>
      <div class="field">
        <label for="email">Email</label>
        <input id="email" name="email" type="email" required autocomplete="email" />
      </div>
      <div class="field">
        <label for="message">Message</label>
        <textarea id="message" name="message" rows="6" required minlength="10" maxlength="5000" aria-describedby="message-aide"></textarea>
        <p id="message-aide" class="muted mt-1 text-sm">10 caractères minimum. Pas de données sensibles, je réponds vite et simplement.</p>
      </div>
      <button type="submit" class="btn btn-solid" data-umami-event="contact-submit">Envoyer</button>
    </form>
  </section>
</Layout>
